# 親(UEプロジェクト)の .gitignore は「既定で全ファイル無視 + 拡張子ホワイトリスト」方式のため、
# .ts / .tsx / .mjs / .css などが黙って無視されてしまう。
# このディレクトリ配下ではそれを打ち消し、必要なものだけを改めて無視する。
!*

node_modules/
dist/
*.db
*.db-journal
*.db-wal
*.db-shm
*.log
.DS_Store
dist-standalone/
